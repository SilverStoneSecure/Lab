# Mon-May-25-2026

## Session Summary
- Authenticated Claude Code on T420 and transferred credentials to silverstone via `scp`
- Discovered credentials file lives at `~/.claude/.credentials.json`
- Accidentally exposed live tokens in chat — rotated via OAuth re-login on silverstone
- Installed VS Code on T420 via Microsoft apt repo (troubleshot heredoc and line-wrap paste issues)
- Generated SSH keypair (`id_ed25519`) and set up passwordless SSH to silverstone
- Configured `~/.ssh/config` with a `Host silverstone` entry
- Installed Remote-SSH extension via `code --install-extension`
- Connected VS Code on T420 to silverstone remotely — filesystem is silverstone's
- Opened project at `/home/chad/Public/www/Lab` in VS Code
- Next step: open integrated terminal in VS Code and run `claude` on silverstone

## Suggested Skills
- **ssh-setup**: Automate SSH keypair generation, config entry creation, and `ssh-copy-id` for a new host
- **vscode-remote**: Install VS Code, Remote-SSH extension, and connect to a named host end-to-end
- **claude-credentials-transfer**: Safely copy Claude Code credentials to a remote server via scp
- **credential-rotation**: Detect exposed tokens and walk through OAuth re-auth for Claude Code

## Chad's Preferences
- Prefers command line over GUI where possible
- Likes steps numbered for easy reference during execution
- Wants commands broken into small paste-able chunks — avoids long one-liners
- Familiar with older dev tools (Visual Studio early 2000s) — frames new tools in familiar terms
- Direct and concise — doesn't want over-explanation
- Prefers to confirm success at each step before moving on
- Likes to understand what something is called, not just how to do it
